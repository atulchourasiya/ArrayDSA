void swap(char *a, char *b)
{
    char temp = *a;
    *a = *b;
    *b = temp;
}
void reverseString(char *s, int sSize)
{
    int size = sSize / 2;
    for (int i = 0; i < size; i++)
    {
        swap(&s[i], &s[(sSize - 1) - i]);
    }
}
